name: Sync Fork with Upstream

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:    

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync fork
        uses: tgymichi/fork-sync@v1.5
        with:
          owner: hydy100
          repo: R3nzSkin
          head: main
          base: main
